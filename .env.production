# Production билд (npm run build) — попадёт в bundle для Vercel.
# Перед первым деплоем замени URL на реальный Railway URL и закоммить
# (или задай через Vercel UI; UI имеет приоритет над .env.production).
VITE_API_URL=https://tushun-api.up.railway.app
