# Локально: указывает на Flask backend на твоей машине
VITE_API_URL=http://127.0.0.1:5000

# Production (Vercel): указывает на Railway backend.
# Эту переменную нужно прописать в Vercel → Settings → Environment Variables
# VITE_API_URL=https://tushun-api.up.railway.app
